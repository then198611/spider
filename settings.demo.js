/**
 * config
 * sql
 * redis
 * port
 * ...
 */

const S = {
  SQL: {
    DATABASE: 'ai_act',
    USER: 'admin',
    HOST: '127.0.0.1',
    PORT: '3306',
    PASSWORD: 'admin'
  }
};

module.exports = S;
